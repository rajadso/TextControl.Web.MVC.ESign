@using TXTextControl.Web.MVC

@{
    ViewData["Title"] = "Prepare the Agreement";
}

<div class="position-fixed bottom-0 end-0 p-3" style="z-index: 5">
    <div id="liveToast" class="toast hide text-white bg-success" role="alert" aria-live="assertive" aria-atomic="true">
        <div class="toast-header">
            <span class="material-icons">
                fact_check
            </span>&nbsp;
            <strong class="me-auto">Text Control eSign</strong>
            <button type="button" class="btn-close" data-bs-dismiss="toast" aria-label="Close"></button>
        </div>
        <div id="liveToastMessage" class="toast-body">

        </div>
    </div>
</div>

<div id="editor"></div>

<section id="home" class="container section">

    <h2>New Agreement</h2>

    <div class="row mt-5">

        <div class="col-md-8 box highlight box-row" id="processSteps">
            <h4>Upload or create a new agreement</h4>

            <div class="row">
                <div class="col-md-1 offset-1 status-border">
                    <span id="statusDocument" class="status status-active"><span class="material-icons">description</span></span>
                </div>
                <div class="col-md-8 offset-md-1 status-content">
                    <p>
                        <strong>Agreement document</strong>
                        <br />
                        Please upload or create a new agreement.
                    </p>
                    <div id="collapseContract">
                        <div id="contractUploadBox">
                            <div class="box-frame">
                                <div class="mb-3">
                                    <input onchange="TextControl.esign.addAgreement(this.files)" accept=".docx, .doc, .rtf, .tx" id="files" name="files" type="file" class="form-control file">
                                </div>
                                <div class="mb-3">
                                    or...
                                </div>
                                <div>
                                    <input onclick="TextControl.esign.createNewAgreement(false)" type="button" class="btn btn-warning" value="New Agreement" />
                                    <input onclick="TextControl.esign.createNewAgreement(true)" type="button" class="btn btn-warning" value="Use Sample Agreement" />
                                </div>
                            </div>

                            <div class="mt-3 waitstate">
                                <div class="spinner-border text-primary" role="status">
                                    <span class="visually-hidden">Loading...</span>
                                </div>
                            </div>
                        </div>

                        <div id="contractPreviewBox">
                            <img class="img-fluid thumbnail-image thumbnail-small" src="" id="statusDocumentThumbnail" />
                            <p class="mt-3" id="statusDocumentInfo"></p>

                            <p>Agreement successfully created. You can modify the agreement in the overview.</p>

                            <a asp-action="index" asp-controller="agreement" class="btn btn-warning mb-3"><strong>Agreement Overview</strong></a>
                        </div>

                    </div>

                </div>
            </div>

        </div>
    </div>



</section>

@section scripts {
    <script>
        
    </script>
}