@model List<Agreement>

@{
    ViewData["Title"] = "Agreement Overview";
}

<section id="home" class="container section">

    <a asp-action="create" asp-controller="agreement" class="btn btn-warning mb-3"><strong>New Agreement</strong></a>

    <h1>Agreements</h1>

    <div class="row">
        <div class="col-12">
            <div>
                <table class="table table-hover mt-5">
                    <thead>
                        <tr>
                            <th class="align-middle" scope="col">Subject</th>
                            <th class="align-middle" scope="col">Created</th>
                            <th class="align-middle" scope="col"></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var agreement in @Model) {
                            <tr>
                                <th class="align-middle" scope="row">
                                    <a asp-controller="agreement" asp-action="summary" asp-route-id="@agreement.AgreementID">@agreement.Name</a>
                                </th>
                                
                                <td class="align-middle">@agreement.Created</td>
                                <td class="align-middle">
                                    <div class="btn-group">
                                        <a asp-controller="agreement" asp-action="summary" asp-route-id="@agreement.AgreementID" class="btn btn-primary">Summary</a>

                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>

</section>

